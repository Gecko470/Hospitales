@model Hospitales.Clases.SedeCLS

@{
    ViewData["Title"] = "Sedes";
}

<h1>Edit</h1>
<hr />
<div class="row">
    <div class="col-md-6">
        <form asp-action="Guardar" asp-controller="Sede" method="post" id="frm">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input asp-for="Iidsede" class="form-control" type="hidden" />
            <div class="form-group">
                <label asp-for="Nombre" class="control-label"></label>
                <input asp-for="Nombre" class="form-control" />
                <span asp-validation-for="Nombre" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Direccion" class="control-label"></label>
                <input asp-for="Direccion" class="form-control" />
                <span asp-validation-for="Direccion" class="text-danger"></span>
            </div>
            <div class="form-group d-flex justify-content-between align-items-center mt-4">
                <a asp-action="Index" asp-controller="Sede" class="btn btn-secondary">Cancelar</a>
                <input type="button" value="Aceptar" class="btn btn-primary" onclick="abrirModal()"/>
            </div>
        </form>
    </div>
</div>

@section Scripts{
    <script>

        function abrirModal() {
            mostrarModal().then((result) => {
                /* Read more about isConfirmed, isDenied below */
                if (result.isConfirmed) {
                    document.getElementById("frm").submit();
                } else if (result.isDenied) {
                    Swal.fire('Los cambios no se han guardado', '', 'info')
                }
            })
        }
    </script>
}


